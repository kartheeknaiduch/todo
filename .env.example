# Environment variables for Netlify deployment
# You need to set these in your Netlify dashboard under Site Settings > Environment Variables

# MongoDB Atlas connection string
MONGODB_URI=mongodb+srv://tgfire05:Fire%400507@todo.f9czfzp.mongodb.net/ToDoList?retryWrites=true&w=majority&appName=ToDo

# JWT Secret for authentication (use a strong, unique secret in production)
JWT_SECRET=FireReddy

# Gmail configuration for email notifications
EMAIL_USER=tgfire05@gmail.com
EMAIL_PASS=vzvanglazskygouy

# Port (Netlify Functions will handle this automatically)
PORT=9999
